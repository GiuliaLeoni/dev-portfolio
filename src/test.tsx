export const Test = () => {
    return (
        <section style={{marginTop: '5rem'}}>
            <h1 style={{color: "white", fontSize: "clamp(3.5rem, 12vw + 1rem, 7rem)"}}>test</h1>
        </section>
    )
}
